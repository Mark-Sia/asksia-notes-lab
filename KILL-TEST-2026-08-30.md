# 5-path kill test：把「格式 + 内联图」送进 Apple Note 的最小交互路径
2026-08-30 · Kai · 全部实测（iPhone 26.6.1 经 iPhone Mirroring + macOS 15.6 Shortcuts/Notes）

> 任务口径已按指令切走 Markdown 图片语法，不再研究 `![]()`。
> 成功判据 = **note 里图是内联渲染的**（不是附件、不是链接）**且**文字保留 heading/bold/list。

---

## 结果矩阵（7 条，全部跑过）

| # | 路径 | 文字 | 格式 | 内联图 | 用户动作 | 判定 |
|-|-|-|-|-|-|-|
| 1 | Safari 网页 → 分享 → 备忘录 | 仅链接 | ❌ | ❌ | 3 | ❌ |
| 2 | `.html` 文件 → 分享 → 备忘录 | — | — | — | — | ❌ **备忘录根本不出现在分享面板** |
| 3 | **富文本对象**（非文件）→ Share → 备忘录 | ✅ | ❌ 拍平 | ❌ | 3 | ❌ |
| 4 | Shortcuts `Get Rich Text from HTML` → `Create Note` | ✅ | ❌ 拍平 | ❌ | 1 | ❌ |
| 5 | **Shortcuts `Create Note` + `Add File`** | ✅ | ❌ 拍平 | ✅ **内联** | **1，零选择器** | ⭐ 新能力 |
| — | `.md` → 分享 → 备忘录 → 导入 | ✅ | ✅ **完整** | ❌（降级成链接） | 3 | 现行方案 |
| — | 复制富文本 → 粘贴 | ✅ | ✅ | ✅ | 4 | 保真但最费事 |

**没有任何一条同时拿到「格式 + 内联图 + 少交互」。** 但 5 号解锁了一个真能力。

---

## 各路径的证据

**1 · Safari 页 → 分享 → 备忘录**：产出一条「1 个网站链接」的 note（只有链接卡片，正文不进）。

**2 · `.html` 文件**：分享面板里只有 隔空投送 / 信息 / 邮件 / 无边记 / 保存到"文件" / 询问 ChatGPT——**备忘录不出现**。
→ Apple 官方文档里的 `.html` 导入**只在 Mac 的 File → Import 菜单有效**，iOS 分享扩展不接受该 UTI。

**3 · 富文本对象（你说的 NSItemProvider 那条）**：Shortcut `Get Rich Text from HTML` → `Share` → 面板里**备忘录在**（说明它确实接受 attributed content item，不是文件），保存后 note 正文 3,827 字符但 **`<li>`/`<b>`/`<h2>` 全为 false**，图不在。
→ **富文本对象能传，格式传不过去。**

**4 · Shortcuts → Create Note**：同上拍平。绑定方式两种都试过：对象绑定（`WFTextTokenAttachment`）→ **正文完全空**；字符串绑定（`WFTextTokenString`）→ 文字进但拍平。
→ 印证 Apple 社区："Notes actions in Shortcuts are built for plain text usage only."

**5 · ⭐ `AddFileAttachmentLinkAction`（"Add File"）**：把下载的 jpg 加进同一条 note，实测 note body =
`<div><img style="max-width:100%..." src="data:image/jpeg;base64,...">` —— **真·内联图，不是附件 chip，且 `openAppWhenRun=false`，全程零 UI**。

---

## 转换发生在哪一层（根因）

不是 Notes 拒绝富文本，而是**上游 `Get Rich Text from HTML` 的产物在跨进程传递时已被降为 plain**：无论后面接 `Create Note`、`Append`、还是 `Share`，结果都一样拍平。
唯一保住格式的转换器是 **Notes 自己的 Markdown importer**（系统级、只吃 `.md`、明确不解析图片资源）。
两个转换器**互斥**：一个保格式不保图，一个保图不保格式。

---

## 因此有一条前面没提过的新选项（5 号解锁的）

**A · 一键零选择器**：Shortcut 一次跑完 → `Create Note`（纯文本正文）+ `Add File`（Sia 图 **内联**）+ 可再 `Add File` 一张**整份秘籍渲染成的图卡**。
用户动作 = **点一下**。没有分享面板、没有 Import、没有粘贴。
代价：正文无 bold/heading（但可搜索、可编辑、可加自己的笔记）。
**视觉冲击由顶部那张图承担 —— 这正是你要图的原因。**

**B · 现行**：`.md` 导入 → 格式完整，图是链接。动作 = 点 → 选备忘录 → 导入。

**C**：复制粘贴 → 全保真，动作最多。

---

## 我的建议

**做 A，和 B 并排放，量一周 CTR。**
理由：A 是唯一真正的「一键」，而且图能内联；B 是唯一能给「真排版」的。这两个代表两种不同的产品主张（*震撼* vs *可编辑*），哪个更被点击是可测的，不该我替用户猜。

C 保留为 power-user 兜底。

---

## 现场已清理

- 测试 note：**19 条全删**（含"最近删除"里的残留），你的 Notes 干净了
- 测试 Shortcut：**全删**（`shortcuts list | grep AskSia` = 0）
- 控制脚本 `iphone.sh` 保留（越界拒绝护栏实测有效，镜像断开时正确挡下点击）
